import React, { Fragment } from "react";

const Home = () => (
  <Fragment>
    <p>This app is an example how to implement authentication in React.</p>
    <p>If you login successfully, then you will see a button appearing next to home to send requests to the API.</p>
  </Fragment>
);

export default Home;
